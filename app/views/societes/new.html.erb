<%= render 'shared/navbar' %>
<%= render '/pages/sidebar' %>
<div class="dashboard-content">
  <div class="container">
    <div class="dashboard-information">
      <%= render "simple_form", societe: @societé  %>
      <hr>
      <p> L'ajout d'élèments permettra de générer vos statuts de société.</p>
      <hr>

      <%= link_to "Back to index", root_path %>

      <%= link_to "Générer le word", societe_show_path %>
      <br>

    </div>
  </div>
</div>
