<%= render 'shared/navbar_sansachat' %>
<%= render '/pages/sidebar' %>
<div class="dashboard-content">
  <div class="container">
    <div class="dashboard-information">
      <%= render "simple_form", clients: @client, locals: { user: @user } %>
      <hr>
      <p> L'ajout de factures permettra de calculer votre chiffre d'affaires.</p>
      <hr>
      <%= link_to "Remplissez vos factures", invoice_new_path %>
    </div>
  </div>
</div>

